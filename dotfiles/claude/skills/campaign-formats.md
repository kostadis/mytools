# Campaign Formats

Read /home/kroussos/CampaignGenerator/docs/formats.md now.

This is the canonical reference for all input/output file formats used by CampaignGenerator tools (session_doc.py, vtt_summary.py, distill.py, campaign_state.py, planning.py, party.py, session_doc_ui.py, prep.py, campaignlib.py).

Use it as your primary reference when:
- Debugging why a tool fails to read a file
- Creating or editing campaign files manually
- Checking what sections/fields a file format requires
- Understanding what a tool writes and what reads it

Do not re-read source files to discover formats — they are all documented in formats.md. If a format in formats.md seems inconsistent with what you observe in actual files, flag it to the user and offer to update formats.md.

After reading the file, confirm you have loaded the formats and are ready to help with campaign work.
