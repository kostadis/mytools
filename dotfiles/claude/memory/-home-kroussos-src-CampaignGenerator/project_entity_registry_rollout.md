---
name: project_entity_registry_rollout
description: HANDOFF — entity-registry rollout (issues
metadata: 
  node_type: memory
  type: project
  originSessionId: d46f1423-7d20-4794-8880-450536506402
---

Building the campaign-entity registry per plan `~/.claude/plans/virtual-swimming-castle.md` (Opus orchestrates, Sonnet implements, Opus reviews every diff + real-data smoke). Repo `/home/kroussos/src/CampaignGenerator`. Fixes the fragmentation bug in [[project_alias_fragmentation]]. Design + check-mechanism rationale in [[project_registry_import_order_and_check]].

## ▶ RESUME HERE (updated 2026-07-10)

Phase 5 **OOTA DONE+merged; Phandalin DONE+merged; toee DONE, PR #68 open.** CG `main`: `merge` verb (#139) + import-dedup fix (#140) + import-alias-decisions rejected-cluster fix (#142). campaigns `main`: OOTA #66 + Phandalin #67. **toee registry = campaigns PR #68 (branch `feat/entity-registry-toee`, commit `b31cd45`), 923 ent, 193 aliases, 6 rejected + 1 distinct guard, grouping-drift 0.**

**toee (this session) — the NEW wrinkle: no pre-built inventories existed.** Unlike OOTA/Phandalin (hand-authored `*inventory*.md`), toee had only raw 5etools JSON + a STALE flat `notes/proper_nouns_adventure.md` (GM rejected it — plain `- Name` bullets, `import-inventory` needs `**Name**`, and its Creatures(114) section is generic monster types). So I **ran `gm-module-inventory` fresh** on 3 module JSONs → typed `docs/background/*-inventory.md`. Reusable harness (in scratch): `fivetools_render.render_entries_block` flattens each 5etools section to text → Workflow fans out 1 Sonnet agent/section (schema-forced JSON) → merge/dedup in Python (skill rule: merge in code, never an agent). Sources: `adventure-t14-5e.json` (T1-4 base, 574 ent), `1802525-TOEE_Leadership_Organization` (363), `1802525-3orcs_TOEE_Village_of_Nulb` (129). Billing gotcha the GM raised: Workflow/Agent subagents bill to the **Claude Code session auth**, NOT `ANTHROPIC_API_KEY`/campaignlib — but if `ANTHROPIC_API_KEY` is set in env when Claude Code launches it may use API billing; confirm with `/status`.

**toee GM catches (identity = human checkpoint, all caught at review):** (a) extractor packed **Barkinar** (distinct commander) into Deggum's alias bullet — but `import-inventory` **collision-skips** a false-alias whose name has its OWN bullet (empirically verified; the insidious class is a packed name with NO own bullet), so it didn't fuse; kept distinct + `distinct` guard, and fixed the source bullet. (b) **Calmert** = church underpriest the party **recruited into itself** → same person as the PC the GM kept misspelling "Calmer"; folded (canonical Calmert, alias calmer). (c) **Dela=Dala=Dalia** one woman (base module has only "Dala"; = the woman Jorin Pike Kale was jailed over). (d) disguise folds Selentis←Nysera Krivaltis, Mother Screng←Y'dey. Adapted smoke again (no facts corpus): drift 27→0, 1116 surface forms→923 canonicals.

**#142 (this session):** `cmd_import_alias_decisions` had the SAME rejected-cluster flattening bug #140 fixed in `import-dedup`, unfixed in this 2nd importer. Phandalin is the first campaign with a `.alias_decisions.json` → tripped it: 28 wrong guards (Harbin↔Harbin Wester, Meril↔Meryl, dragon↔Cryovain…). Fix = `_distinct_rejected_reps_by_name` (registry-identity collapse; import-alias-decisions runs LAST so confirmed dups already resolve together). 3 tests. Verified 28→0 on real data.

**Phandalin recipe extras vs OOTA:** (a) 4th importer `import-alias-decisions docs/ensemble/.alias_decisions.json` (present here, absent OOTA) — run it LAST, after frontmatter. (b) Neverwinter is a real clean `*inventory*.md` but lives in `notes/`; GM chose Dragon+Neverwinter → **copied `notes/neverwinter/neverwinter-inventory.md` → `docs/background/`** (import==check symmetry). (c) NO atomic-facts corpus exists → OOTA-style `facts_to_state --list` smoke impossible; used **adapted smoke** (actionable-fragmentation 14→3 where all 3 residuals are deliberate GM distinctions = 0 unresolved; alias-fold 695→564). (d) **`entities.json` at campaign root = drive-tagger noun dump, NOT a source.**

**Phandalin GM landmine (the key catch):** the ensemble `.alias_decisions.json` **wrongly fused two distinct plot staves** — `Meril's Staff` (Soma's mentor's sentient staff) vs `Staff of Bird Calls` (separate ch45 acquisition). Textbook LLM-grouping-got-identity-wrong; caught at human checkpoint. Registered Meril's Staff as its own item + `distinct` guard. Also: `obsidian sword` = the Carver's true form (remnant held by Vukradin, ownership open) → deferred to triage, NOT folded.

**NEXT:** OOTA/Phandalin/toee all done — no more campaigns queued. If a new campaign appears: same recipe (init → import-inventory×N → import-dedup → import-frontmatter → [import-alias-decisions] → check → GM `merge`/`mark-distinct` → project → smoke → PR). **If it has no hand-authored `*inventory*.md`, reuse the toee harness**: `gm-module-inventory` on its module JSON(s) via the Workflow fan-out (scratch scripts `flatten_*.py` + `merge_toee_inv.py` + `alias_group_review.py` — the alias-group review classifies the ~N multi-bold groups, flags only bare-name personas w/ no own bullet). Merge campaigns PR #68 when the GM approves.

**Footnotes for toee:** (1) **inventory mirror-drift** — copying an inventory into `docs/background/` creates a 2nd copy (Phandalin now has neverwinter-inventory in BOTH `notes/neverwinter/` and `docs/background/`; OOTA has the `docs/inventory/`↔`docs/background/` mirror). Pick a source-of-truth per file so future edits don't diverge. (2) **PCs land in the registry** — `import-frontmatter` globs all of `docs/npcs/` incl. `pc_files_skipped`; harmless (facts_to_state unions party.yaml separately), consistent OOTA↔Phandalin, but the registry is not PC-free.

**Also pending (not blocking):** notes-discovery pass (`triage-candidates` + entity-triage skill) for homebrew-only entities in `notes/` (e.g. OOTA's **House T'sarran** faction — in no inventory/dossier; its members ARE registered). No un-reject CLI verb (minor). **CG issue #141** — reconcile the `vtt-spell-pass` skill's `notes/vtt_transcription_corrections.md` (wrong→right glossary = alias data) + `notes/vtt_known_additions.md` (new-noun discovery) with the registry; spelling-correction moved upstream (VTT) from downstream (the old `.dedup_state.json` was a downstream-dedup artifact) → two authorities now, decide direction of truth.

**Env gotcha:** tests + `registry.py` need `/home/kroussos/.venv/bin/python` (system `python3` has no pytest; there is no `python`). Run `registry.py` from the CG tree; campaign dir is the arg.

## State as of 2026-07-08 — PHASES 1–3 ALL MERGED TO MAIN

The whole stack **#131→#132→#133→#134 is MERGED to `main`** (tip `f49893e`). main had diverged ~25 commits (to #136) since the stack branched, so each branch was rebased-onto-main (`git rebase --onto origin/main <old-base>`, replaying only per-phase deltas) before merge. Conflicts, all resolved: (a) `ensemble_merge.py` (all branches) — took phase1's `from campaignlib.textproc import norm_subject`, kept main's `import campaignlib` (still needed for `atomic_write_json`), dropped main's local `_norm_subject` def; (b) `facts_to_state.py` (phase2) — **keep-both** (main's `_collect_monster_vocab` + phase2's `_load_pc_names`/`_resolve_registry_path`, no logic overlap) PLUS a silent auto-merge NameError: merged `main()` spliced main's `parser = build_parser()` with phase2's `p.error`/`_resolve_registry_path(args, p)` → `p` undefined; fixed both refs to `parser`. Semantic finding: phase2 never touched the `is_known` ladder (100% main's); registry-fed `known_names` flows into precedence step 2 — composes cleanly. **BUT** main's `b0e8063` "named npc is known by default (unless also a monster)" heuristic independently masks npc fragmentation, so phase2's fragmentation-demo test now passes trivially for npcs — re-pointed the paired demo tests to a **faction** (`Zhentarim`↔`The Black Network`) where the heuristic doesn't apply. Registry-vs-legacy smoke passed end-to-end (legacy disagreement → 2 location-scoped bundles; registry → 1 known + PC-name folding). Merged-main suite: **1087 passed**, baseline-only reds (see below). NOTE: `gh pr edit --base` is broken here (projects-classic GraphQL error) — retarget with `gh api -X PATCH repos/.../pulls/N -f base=main`. Backup tags `backup/phase{1,2,3,3b}-preRebase` exist locally; remote feature branches NOT deleted.

mytools **PR #103** (entity-triage skill) **MERGED to mytools main 2026-07-08** (tip `e46ed0a`; skill at `dotfiles/claude/skills/entity-triage/SKILL.md`). Verified dependency-complete against merged CG main (`triage-candidates`/`add --yes`/`alias --to`/`mark-distinct`/`mark-rejected`/`check`/`project`/`init` + `fivetools_catalog.py search` all exist). The branch had accreted two unrelated commits — force-rebased to origin/main + the single skill commit before merge: **dropped** `6f6a58c` (pdf-translators multi-select — a superseded dup, already merged separately via #104) and `4159c3a` (`ARCHITECTURE.md`, GM chose drop-from-PR; NOT on mytools main — re-add via its own docs PR if wanted). Unrelated working-tree WIP (`dotfiles/claude/CLAUDE.md` mod + untracked `codebase-memory/` skill dir) preserved via stash across the rebase — still uncommitted, not mine. `gh pr edit` is broken here too (projects-classic GraphQL) — edited body via `gh api -X PATCH repos/kostadis/mytools/pulls/103 -F body=@file`.

## Phase 5 — OOTA migration DONE + `merge` verb (2026-07-08)

**`merge` verb — CampaignGenerator PR #139** (branch `feat/entity-registry-merge-verb`, off main `356c098`). The registry had `alias`/`mark-distinct`/`mark-rejected` but NO positive "these two EXISTING entities are the same — fold them into one" — the operation "GM resolves grouping drift" actually needs (inventory spelling + dossier spelling each register as their own entity; `alias` refuses to move an owned surface form). `registry.py merge <dir> --into "<canon-or-alias>" other...`: resolves via `_find_owner`, atomic pre-check, **respects anti-merge guards** (refuses if a `distinct` pair or shared `rejected_aliases` group links target↔other — `validate` catches distinct indirectly but NOT rejected, so `_guard_blocks_merge` checks both before mutating), folds name+aliases+provenance (target wins on type mismatch, warned), removes folded entities, validate-then-save. 13 tests; full suite 1112 passed (baseline reds only). **Not yet merged.**

**OOTA registry BUILT — campaigns repo branch `feat/entity-registry-oota`, commit `241129f` (NOT pushed).** 954 entities from BOTH modules (Out of the Abyss + Candlekeep Murders) + caches, canonical order init→import-inventory×2→import-dedup→import-frontmatter. Multi-module: shared FR entities (Faerûn/Waterdeep/planes) unify. Candlekeep source pre-fixed: split 7 bullets that packed DISTINCT entities into false-alias groups (Archibald Sr/Jr, 2 Galleries, Nine Hells/Avernus, 2 instant-summons, House of Rest/Hearth, 4 Avowed ranks, 6 Deadwinter traditions) — NOTE `_parse_bullet` splits on `" — "` and only reads bold spans BEFORE the em-dash, so `X — contains **A**, **B**` is SAFE (A/B land in the note); only multi-bold BEFORE the dash fuses. `docs/inventory/` mirror synced. 6 GM-approved merges (Plinki←Pliinki, Naomi←Nomi, Jadgar←Burrow Warden Jadger cluster, Nym Duskryn←Nym, Fuurm Coppernose←"Copper" variant, Demonweb Pits←Demonweb). check after: grouping drift **none**; 47 fuzzy (review-only). Smoke (facts_to_state --list, registry vs no-registry heuristic): alias folding consolidates fragments to canonical (Asha Vandry+Vandree+Asha→Asha Vandree; 4 Ilvara(loc) fragments→Ilvara Mizzrym; Sathir→Sarith Kzekarit) — bundles **745→690**. NOTE the known **2639→629** is heuristic REPLACEMENT (authoritative known-set supersedes "every named npc known"), NOT folding — advisor caught this misread; total entities RISES 2811→2946 as ~2000 one-offs re-count per location-scope. Verified no regression: top ~27 demoted names all IN registry under canonical form; only genuinely-absent = Derro/Myconids (creature TYPES) + homebrew faction House T'sarran (no inventory/dossier → deferred notes-discovery; its members ARE registered).

**BUG FOUND + FIXED — `import-dedup` flattened a rejected CLUSTER too broadly. CG PR #140** (branch `feat/registry-import-dedup-rejected-fix`, off main). dedup `clusters_rejected` `{aliinka,plinki,pliinki}` ("Aliinka separate; only plinki/pliinki are duplicates") → imported as flat `rejected_aliases [Aliinka,Plinki,Pliinki]`, whose pairwise expansion WRONGLY forbade Plinki↔Pliinki (the pair the SAME store's `clusters_confirmed` says merge). The `merge` guard correctly refused → surfaced it. **Fix:** before emitting the guard, collapse a rejected cluster's members to one rep per DISTINCT entity — two collapse if they share a confirmed-cluster canonical (`_confirmed_canon_map`) OR already resolve to the same registry entity (catches the Ilvara twin, aliased by inventory not dedup); guard only across survivors; if all collapse, add none (warn). Verified on real OOTA data: `[Aliinka,Plinki,Pliinki]→[Aliinka,Plinki]`, `[Ilvara,Ilvara Mizzrym,Ulnara]→[Ilvara Mizzrym,Ulnara]`; the 4 genuine 2-way rejections unchanged. 3 tests. **Aliinka STAYS separate** (guard `[Aliinka,Plinki]` kept). With #139+#140, a fresh migration resolves spelling-frag with no manual guard surgery. NOTE the already-committed OOTA registry (241129f/2307772) still carries the hand-fix for Plinki + the dormant `[Ilvara,Ilvara Mizzrym,Ulnara]` group — a fresh rebuild once #140 lands would produce clean guards.

**ALL MERGED (2026-07-08/09).** CG `main`: **#139 merge verb** + **#140 import-dedup fix** (tip `6a8b3af`). campaigns `main`: **#66 OOTA registry** (tip `95b7315`, `docs/entity_registry.yaml` 954 entities). After #139+#140 landed, the OOTA migration was **re-run fresh from merged tooling** (amended `60e303f`) so the registry is fully tool-generated — no hand-fix: all 6 merges applied via `merge` cleanly (Plinki←Pliinki no longer blocked), guards clean (`[Aliinka,Plinki]`, `[Ilvara Mizzrym,Ulnara]` — dormant Ilvara-triple gone, manual distinct[Aliinka,Plinki] gone). check drift=none, smoke 745→690.

**Phase 5 = OOTA DONE.** Next campaigns: **Phandalin, toee** — same recipe (enumerate `docs/background/*inventory*.md` per campaign, import each → dedup → frontmatter → check → GM `merge`/mark-distinct → project → smoke). Toolkit is complete now (merge + import-dedup fix on main).

**Still-open loose ends:** notes-discovery pass (triage-candidates + entity-triage skill) to catch homebrew-only entities like **House T'sarran** (faction in no inventory/dossier; its members ARE registered). No un-reject/un-mark CLI verb (minor gap; not needed now that import-dedup emits correct guards). Stale remote branches: CG `feat/entity-registry-phase{1,2,3,3b-alias}` + the 3 just-merged (`feat/entity-registry-merge-verb`, `feat/registry-import-dedup-rejected-fix`) + campaigns `feat/entity-registry-oota` + local `backup/*-preRebase` tags — safe to delete.

## (historical) State as of 2026-07-07 (Phase 3 complete, pre-merge)

**Phase 3 finished this session (both committed LOCALLY, NOT pushed):**
- **alias verb** — CG branch `feat/entity-registry-phase3b-alias` (off phase3), commit `fd6186e`. Non-interactive `registry.py alias <dir> --to "<canonical-or-alias>" variant...`: closes Gap B (`add --yes` can only mint NEW entities; the only alias path was the interactive `[1]` prompt a skill can't drive). Atomic (all variants pre-checked; collision aborts writing nothing), idempotent, validated. 7 CLI tests; 177 registry+isolation green.
- **entity-triage skill** — mytools branch `feat/entity-triage-skill`, commit `631b965`, at `dotfiles/claude/skills/entity-triage/SKILL.md`. Consumes `triage-candidates` queue JSON. Four rulings → new(`add --yes`) / alias-of(`alias --to`) / not-an-entity(skill-side state) / defer(state). Hybrid cadence (batch no-near-miss, converse near-miss/high-stakes). Generic-noun evidence: **`fivetools_catalog.py search` is the backbone** (5etools MCP only source/TOC/section, no confirmed name-lookup → optional accelerator only — GM confirmed this framing). Distinct-but-similar = `add` + `mark-distinct`. State file `docs/.entity_triage_state.json`.

Both PUSHED + PRs open: **CG PR #134** (alias verb, base `feat/entity-registry-phase3` = #133) and **mytools PR #103** (entity-triage skill, base main; depends on #134). NOTE: mytools working tree had ~10 unrelated uncommitted changes at push time — not touched, not mine.

**Earlier — done + pushed as stacked PRs (all base off `main` via `feat/entity-registry-phase1`):**
- **Phase 1 — PR #131** `feat/entity-registry-phase1`: `campaignlib/registry.py` loader (Entity/Registry, load_registry validate, projections) + top-level `registry.py` CLI (`init`/`add`/`project`/`import-inventory`/`import-dedup`/`import-frontmatter`/`import-alias-decisions`/`check`) + `norm_subject` moved to `campaignlib/textproc.py`. Pure addition.
- **Phase 2 — PR #132** `feat/entity-registry-phase2` (stacked on phase1): `facts_to_state.py --registry` (single source for aliases + known_names; PC union from party.yaml; legacy flags deprecated; conflict error). THE bug-fix, proven by regression test.
- **Phase 3 (CG) — PR #133** `feat/entity-registry-phase3` (stacked on phase1, independent of phase2): `registry.py triage-candidates` (session-output proper-noun diff → queue JSON; PC-union; near-miss hints), `mark-distinct`/`mark-rejected` (validated negative-writers), `spell_canon.proper_noun_counts` text-core refactor.

All: full suite **1000 passed**; the only red is a PRE-EXISTING dgxlib baseline (3 failed + 5 errors in test_dgx_registry.py + test_extract_facts.py::test_cli_parallel_fully_cached — `campaignlib/api/backends.py` missing `import sys` masks a `dgxlib`-not-installed env issue; GM chose to leave it). CI isolation guard green throughout.

## Next up (resume here)

1. **Phases 1–3 MERGED to main** (see State section). Stack landed 2026-07-08.
2. **Phase 3a is DROPPED** (no `import-source`). Resolved via existing `gm-module-inventory` (5etools source_id or source.md → typed inventory.md) → `import-inventory`. Doc it in Phase 4.
3. **Phase 4 — MERGED to main (PR #138, 2026-07-08; main tip `356c098`).** Consumers now on the registry. Packets: **4A** `campaignlib.party` (`load_party_names`+`load_pc_names`), both `_load_pc_names` copies removed. **4B** `npc.load_alias_map(dossier_dir, registry_path=None)` — registry **REPLACES** dossier scan (single-authority; decided, not merge); 8 call sites (7 CLIs auto-discover CWD via `find_alias_registry` which prints an adoption notice; `connections.py` derives from request dossier-root, stays silent). registry.py's own 2 `load_alias_map` calls (import-frontmatter, check) deliberately stay dossier-only (circular otherwise) — commented. **4C** `--registry` on synthesise_world_state/facts/polish via hoisted `campaignlib.registry.resolve_registry_arg` (facts_to_state retrofitted to it too); **aliases only — `--inventory` (module-canon grounding) left untouched, confirmed a different axis**. **4D** `planning --build-dossiers` seeds dossier `aliases:` from registry (normalised lookup). **4E** CLAUDE.md "Entity registry" rule + gm-module-inventory→import-inventory build path. Suite 1099 passed (baseline reds only). Real-data OOTA validation is Phase 5 by design (this PR = unit + `--dump-only` smoke, correct altitude).
4. **Phase 5** — per-campaign migrations OOTA → Phandalin → toee (GM-supervised; each: init → importers → check → GM resolves drift → commit → project → smoke facts_to_state --registry vs legacy).

## Loose ends flagged to GM (not blocking)
- Pre-existing baseline reds on main (NOT ours; identical on plain `origin/main`): `test_dgx_registry.py` 2 failed + 5 errors + `test_extract_facts.py::test_cli_parallel_fully_cached` (`campaignlib/api/backends.py` missing `import sys`, dgxlib not installed) and `test_resolve_refs.py` 2 failed (`fivetools_data` root unset). GM chose to leave the dgx one.
- `project` writes `docs/aliases.json`; legacy ensemble path is `docs/ensemble/aliases.json` — exact target is a Phase-5 decision.
- Remote feature branches `feat/entity-registry-phase{1,2,3,3b-alias}` still exist (not auto-deleted on merge) + local `backup/*-preRebase` tags — safe to delete once satisfied.
